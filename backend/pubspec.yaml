name: backend
description: A Dart-native backend for scraping and aggregating news.
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.11.5 <4.0.0'

dependencies:
  shelf: ^1.4.1
  shelf_router: ^1.1.4
  http: ^1.3.0
  xml: ^6.6.1
  html: ^0.15.4
  crypto: ^3.0.7
  intl: ^0.20.2
  core:
    path: ../core

dev_dependencies:
  lints: ^6.1.0
  test: ^1.31.1
